# 安全策略

## 支持范围

安全修复优先覆盖主分支和最近发布版本。规划中内容不代表生产承诺，但其中的示例仍不得包含已知危险默认值。

## 私密报告

请使用 GitHub Private Vulnerability Reporting 提交漏洞，不要在公开 Issue、讨论区或拉取请求中披露可利用细节。报告应包含受影响路径、环境、复现步骤、影响判断和建议缓解措施；不要包含与复现无关的真实用户数据。

## 响应流程

维护者确认报告后进行分级、复现、修复、回归测试和披露协调。严重问题优先撤销凭据、限制权限或下线受影响能力，再完成根因修复。未经授权不得测试第三方系统。

## 示例安全

所有密钥通过环境变量注入；工具调用遵循最小权限；网络请求设置超时；高风险、不可逆、资金和隐私动作需要人工确认。财富相关内容只用于工程教育，不构成投资建议。
