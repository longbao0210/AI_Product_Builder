# Repository 目录结构与职责

## 设计目标

本仓库把课程、示例、项目、规范、模板和自动化工具分开管理。每个目录只承担一种主要职责，避免同一信息在多个位置重复维护。新增内容前应先确认它属于系统知识、可运行示例、商业项目还是公共资产，再选择稳定路径。

## 顶层目录

- `.github/`：持续集成、议题模板、拉取请求模板和社区治理配置。
- `assets/`：可复用静态资源；不保存章节专属截图。
- `checklists/`：学习、开发、发布和生产上线的质量门禁。
- `config/`：模块、项目与 Demo 的稳定清单，是编号和成熟度的事实来源。
- `diagrams/`：跨章节复用的 Mermaid 源文件和图表索引。
- `docs/`：四十个编号课程模块，是系统学习主线。
- `examples/`：可独立运行的最小 Demo，使用全局三位数编号。
- `images/`：经过压缩、具备替代文本的图片资源。
- `learning-paths/`：三十、九十、一百八十和三百六十五天学习安排。
- `projects/`：二十五个端到端商业项目及工程实现。
- `prompts/`：经过说明和评测的提示词资产。
- `resources/`：跨模块官方资源与长期参考资料。
- `scripts/`：生成索引、校验结构和执行维护任务的工具。
- `standards/`：术语、写作、代码、图表、命名和版本规范。
- `templates/`：可复制的学习、产品与工程文档模板。
- `tests/`：校验脚本、生成器和内容契约的自动化测试。

## 维护边界

教学正文只能放在对应课程模块；项目实现只能放在项目目录；工具脚本不能自动生成最终教学观点。文件迁移必须同步修改清单、导航和反向链接。成熟度必须如实标记，规划内容不能伪装为已完成内容。
